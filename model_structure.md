# 模型架構演進分析 (Model Structure Evolution)

## 🧱 目前的架構：Channel Independence (後期融合的悲劇)
想像您有一段 110 幀的影片（如深蹲、硬舉），裡面有 40 個空間特徵（關節）。

* **平行時空拆解**：模型第一步就把輸入 `(Batch, 110幀, 40特徵)` 強行打散成 `(Batch × 40, 110幀, 1特徵)`。這等於憑空創造了 40 個平行宇宙，每個宇宙裡只有一個孤獨的關節在移動。
* **Patching (分塊)**：把這 110 幀切成 12 個小塊 (Patch)。
* **缺乏空間感的 Transformer**：這 40 個宇宙各自進入 Transformer。Attention 機制非常努力地在找「時間上的關聯」，但它從頭到尾都看不見其他關節在哪裡。
* **大雜燴拼接 (Late Fusion)**：等 Transformer 算完後，模型才把這 40 個平行宇宙的個別結論硬生生接在一起，變成一條長達 `10,240` 維的超大特徵向量。
* **過載的分類器 (Overfitting 的元凶)**：這條超大向量進入我們剛才寫的 MLP。為了消化這 10,240 維並憑空猜出「背跟膝蓋的角度」，這個 MLP 暴增了高達 **1,000 萬**個參數！這導致模型瞬間就把整個訓練集的答案死背下來了。

## 🚀 第三階段新架構：Early Fusion Sequence Transformer
打破 CI 後，我們將回歸「電腦視覺」與「動作識別」最正統的主流架構：

* **同框看全身 (早期融合)**：我們不再拆開平行時空！輸入維持原始的 `(Batch, 110幀, 40特徵)`。
* **全息 Patching**：在切 Patch 的時候，我們切下來的每一塊積木，都同時包含了那一瞬間全身 40 個空間特徵。這代表每一個 Patch 本身就紀錄了「姿勢」。
* **擁有上帝視角的 Transformer**：當這些「全息 Patch」進入 Transformer 時，Attention 機制終於開了天眼！它在運算任何時間點時，都在底層直接融合了所有關節的相對幾何位置。
* **極致輕量化的分類器 (解決死背)**：因為 Transformer 已經在內部完美處理了所有空間關係，它輸出的結論只需濃縮在精鍊的 `256` 維裡（不再需要把 40 個通道拼接成 10,240 維）。
* **參數大甩賣**：最後一層 MLP 的輸入從 10,240 銳減到 256。分類器的參數會從一千萬個，斷崖式下跌到只剩約 **3.3 萬**個！
